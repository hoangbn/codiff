# Implementation Plans

Generated by the improve skill on 2026-08-07. Execute in the order below unless
dependencies say otherwise. Each executor must read the plan fully before
starting, honor its STOP conditions, and update the row when done.

## Execution order and status

| Plan | Title                                           | Priority | Effort | Depends on | Status |
| ---- | ----------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Add right-side sidebar support to ReviewSurface | P1       | M      | —          | DONE   |

Status values: TODO, IN PROGRESS, DONE, BLOCKED (with a one-line reason), or
REJECTED (with a one-line rationale).

## Dependency notes

- Plan 001 is self-contained and has no prerequisite plan.

## Findings considered and rejected

- A CSS-only host override was rejected because the embedded shell supplies an
  inline grid template and the pointer hook still measures from the left edge.
- A persisted desktop `sidebarPosition` setting was deferred because the user
  requested the public embedded `ReviewSurface` interface. Desktop config/schema/UI
  work is a separate, larger feature.
- `PlanReviewSurface` support was rejected from this scope because it uses a
  different shell and has no matching resizable review sidebar.
